PORT=3000
LOG_LEVEL=info
NODE_ENV=production

# MySQL (RDS)
DB_HOST=
DB_PORT=3306
DB_USER=admin
DB_PASSWORD=
DB_NAME=migrationdb

# ElastiCache (Redis)
REDIS_HOST=
REDIS_PORT=6379

# Secrets Manager ARNs (preferred over plaintext env)
DB_SECRET_ARN=
